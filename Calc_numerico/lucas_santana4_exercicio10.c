#include <stdio.h>

int main()
{
    printf("A principal diferença entre erro absoluto e erro relativo é que o erro absoluto é a diferença\nentre o valor exato e o valor aproximado, enquanto o erro relativo é a razão entre o erro absoluto e o valor verdadeiro\n");
    printf("\n[Erro Absoluto:\t o valor real do erro, pode ser representado por E = |x - x'|]\n");
    printf("[Erro Relativo:\t o valor percentual do erro, pode ser representado por E = (|x - x'|)/x]\n");
    return 0;
}
